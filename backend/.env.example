# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tamtdb
DB_USER=tamtuser
DB_PASSWORD=TAMTTAMT

# Debug mode: true の場合 JWT をスキップし DEBUG_AID をユーザ ID として使用
DEBUG=true
DEBUG_AID=1

# JWT (DEBUG=false 時に使用。認証 API と同一の値を設定すること)
SECRET_KEY=change-me-to-same-value-as-auth-api
ALGORITHM=HS256
COOKIE_NAME=access_token

# CORS (カンマ区切り)
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
